spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/art_gallery_dev?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.artgallery: DEBUG

